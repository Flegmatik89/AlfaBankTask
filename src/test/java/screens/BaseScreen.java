package screens;

import utils.Swipe;
import utils.Utils;
import utils.Waits;

public class BaseScreen {
    protected Utils utils = new Utils();
    protected Waits waits = new Waits();
    protected Swipe swipe = new Swipe();
}
